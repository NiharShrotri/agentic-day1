# Agentic AI Day 1 Assignment

## Objective
Demonstrate context failure and fix using LangChain message-based invocation.

## Files
- app.py → main implementation
- requirements.txt → dependencies
- .gitignore → excludes sensitive files

## How to run
pip install -r requirements.txt
python app.py

## Note
.env file must NOT be committed as it contains sensitive API keys.
.env not to be committed
The `.env` file contains API keys and must NOT be committed to the repository.